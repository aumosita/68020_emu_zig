68020-emu 프로젝트에 6522 VIA (Versatile Interface Adapter) 프로세서 구현을 추가하였습니다.

### 주요 구현 사항:
1.  **핵심 레지스터 구현**: `PORTA`, `PORTB`, `DDRA`, `DDRB`, `T1`, `T2`, `ACR`, `PCR`, `IFR`, `IER` 등 주요 16개 레지스터의 읽기/쓰기 로직을 구현했습니다.
2.  **타이머 기능**:
    *   **Timer 1**: One-shot 및 Free-running 모드를 지원하며, 카운트 완료 시 인터럽트 플래그 세팅 및 재로딩 로직을 포함합니다.
    *   **Timer 2**: 기본 Timed interrupt 모드를 구현했습니다.
3.  **인터럽트 제어**: `IFR` 및 `IER` 설정을 통한 인터럽트 상태 업데이트와 `irq_pin` 상태 반영 로직을 추가했습니다.
4.  **C API 확장**: 외부 프로젝트(예: M68020 연동)에서 사용할 수 있도록 `via_create`, `via_read`, `via_write`, `via_step` 등의 C export 함수를 `root.zig`에 추가했습니다.
5.  **검증**: `src/via6522.zig` 내부에 기초적인 타이머 동작 테스트 코드를 추가하여 정상 작동을 확인했습니다 (Zig 테스트 71개 모두 통과).

### 파일 경로:
*   구현 파일: `src/via6522.zig`
*   API 노출: `src/root.zig`

추가로 CA1/CA2 핸드셰이크나 시프트 레지스터(SR) 등 세부 기능의 정밀한 구현이 필요하시면 말씀해 주십시오.🧭
